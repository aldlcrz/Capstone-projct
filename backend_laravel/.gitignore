*.log
.DS_Store
.env
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
/.fleet
/.idea
/.nova
/.phpunit.cache
/.vscode
/.zed
/auth.json
/node_modules
/public/build
/public/hot
/public/storage
/storage/*.key
/storage/pail
/vendor
Homestead.json
Homestead.yaml
Thumbs.db

# Uploaded image files are stored as base64 in the DB — no need to commit binaries.
# Keep the folder structure with .gitkeep so fresh clones have the directories ready.
/public/uploads/**
!/public/uploads/**/.gitkeep
