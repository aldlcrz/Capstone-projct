<IfModule mod_rewrite.c>
    RewriteEngine On

    # Redirect root to public/ folder if DocumentRoot points to project root on Hostinger
    RewriteCond %{REQUEST_URI} !^/public/
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
